Within these files, new features are grouped by config.

For example:
- Potion of the Turtle Master is a potion, yet it is in the Mobs section, because
  it is only added if Turtles are enabled in the config.
